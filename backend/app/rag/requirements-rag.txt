# Dependencies required by backend/app/rag
#
# Install into your existing FastAPI backend's requirements.txt.
# No frontend, auth, or CrewAI dependencies are introduced by this module.

langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.2.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0
chromadb>=0.5.0
pypdf>=4.3.0

# Required environment variables:
#   OPENAI_API_KEY          — OpenAI API key used by embeddings.py
#
# Optional environment variables (sensible defaults provided):
#   RAG_CHUNK_SIZE           (default: 1000)
#   RAG_CHUNK_OVERLAP        (default: 150)
#   RAG_EMBEDDING_MODEL      (default: text-embedding-3-small)
#   RAG_CHROMA_PERSIST_DIR   (default: backend/app/rag/chroma_store)
#   RAG_CHROMA_COLLECTION    (default: mediguide_reports)
#   RAG_TOP_K                (default: 4)
